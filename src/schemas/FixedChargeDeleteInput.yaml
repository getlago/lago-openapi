type: object
properties:
  fixed_charge:
    type: object
    properties:
      cascade_updates:
        type: boolean
        description: When set to `true`, the deletion will be cascaded to the children plans.
        example: true
