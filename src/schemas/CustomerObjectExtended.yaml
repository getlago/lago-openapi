allOf:
  - $ref: './CustomerObject.yaml'
  - type: object
    properties:
      metadata:
        type: array
        items:
          $ref: './CustomerMetadata.yaml'
      taxes:
        description: List of customer taxes
        type: array
        items:
          $ref: './TaxObject.yaml'
