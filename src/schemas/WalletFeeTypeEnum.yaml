type: string
description: |
  Fee type to which a wallet can be restricted. It can be any of the following values:
    - `charge`
    - `subscription`
    - `commitment`
    - `fixed_charge`
enum:
  - charge
  - subscription
  - commitment
  - fixed_charge
