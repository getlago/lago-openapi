type: object
description: |-
  Metadata to set on the credit note. Keys are strings (max 40 characters), values can be strings (max 255 characters) or null.
additionalProperties:
  type:
    - string
    - "null"
example:
  external_id: "ext-123"
  synced_at: "2024-01-15"