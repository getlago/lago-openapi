type: object
required:
  - feature
properties:
  feature:
    allOf:
      - $ref: './FeatureBaseInput.yaml'
      - type: object
        required:
          - code
        properties:
          code:
            type: string
            example: "seats"
            description: "Unique code used to identify the feature."
