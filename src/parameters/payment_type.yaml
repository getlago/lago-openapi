name: payment_type[]
in: query
description: Filter by either `manual` or `provider`, matching any supplied type; a single value can also be sent as `payment_type=manual`.
required: false
style: form
explode: true
schema:
  type: array
  items:
    type: string
    enum:
      - manual
      - provider
  example:
    - manual
